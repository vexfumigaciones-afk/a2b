{
  "name": "a2b-by-vex-bridge",
  "version": "1.2.0",
  "private": true,
  "description": "A2B by VEX bridge for Aspel ADM, CFDI delivery and field operations",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check crypto-store.js && node --check aspel.js"
  },
  "dependencies": {
    "playwright": "1.55.0"
  },
  "engines": {
    "node": ">=20"
  }
}
